# front-end-senac

teste

comandos git:

git clone https://github.com/dudedias/hct.git
cd hct
git status

1. git add .
2. git commit -a -m "Texto descritivo" 
3. git push origin main (salva no gh as atualizações)

git pull origin main (baixa do gh as atualizações)
