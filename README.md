# front-end-senac

teste

comandos git:

git clone https://github.com/dudedias/hct.git
cd hct
git status
git add .
git commit -a -m "Texto descritivo" 

git push origin main (salva no gh as atualizações)
git pull origin main (baixa do gh as atualizações)
